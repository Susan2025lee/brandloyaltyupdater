# LLM Proxy Configuration
# Set to "True" to use proxy, "False" to connect directly
USE_LLM_PROXY=True

DEFAULT_LLM_MODEL_KEY="gpt-o4-mini" 

# Default Embedding model key from config.json
DEFAULT_EMBEDDING_MODEL_KEY="text-embedding-ada-002"

# OpenAI API Key (already configured in config.json, included here for reference)
# OPENAI_API_KEY=your_api_key_here 